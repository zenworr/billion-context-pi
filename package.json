{
  "name": "hybrid-acp-workspace",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/kernel",
    "packages/pi-extension"
  ],
  "pi": {
    "extensions": [
      "./packages/pi-extension/dist/index.js"
    ]
  },
  "scripts": {
    "build": "npm run build -w acp-kernel && npm run build -w billion-context-pi",
    "typecheck": "npm run typecheck -w acp-kernel && npm run build -w acp-kernel && npm run typecheck -w billion-context-pi",
    "test": "npm test -w acp-kernel && npm run build -w acp-kernel && npm test -w billion-context-pi",
    "e2e": "npm run e2e -w billion-context-pi",
    "eval": "node evals/replay/launcher.mjs",
    "verify": "npm run typecheck && npm test && npm run build && npm run eval"
  },
  "engines": {
    "node": ">=20"
  }
}
